#include <gtest/gtest.h>

TEST(MyBudgetTest, ExampleTestCase) {

    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(MyBudgetTest, ExampleTestCase1) {

    EXPECT_EQ(1, 1);
    EXPECT_FALSE(false);
}
